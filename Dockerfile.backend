FROM python:3.9-slim

WORKDIR /app

COPY lead-generator/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY lead-generator .

# Cloud Run injects PORT env var
CMD ["python", "main.py"]
